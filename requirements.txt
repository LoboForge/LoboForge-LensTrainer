# Match microsoft/Lens tested stack where possible.
torch>=2.5.0
accelerate>=1.3.0
diffusers>=0.38.0
transformers>=5.8.0
peft>=0.14.0
einops>=0.8.0
huggingface_hub>=0.28.0
safetensors>=0.5.0
pillow>=10.0.0
torchvision>=0.20.0
numpy>=1.26.0
pyyaml>=6.0
tqdm>=4.66.0
bitsandbytes>=0.45.0

# MXFP4 text encoder on GPU (default disable_mxfp4=false). Pin: 0.15+ breaks transformers 5.9.
kernels>=0.12.0,<0.15
triton>=3.4.0

# Microsoft Lens: clone to vendor/Lens (see scripts/install_microsoft_lens.sh).
# Upstream has no pyproject.toml — pip install from GitHub will fail.
# After setup: export PYTHONPATH="${INSTALL_DIR}/vendor/Lens:${PYTHONPATH}"
